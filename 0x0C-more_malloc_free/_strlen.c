#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * _strlen -return string length
 * @string: the string
 * Return: int
 */

int _strlen(char *string)
{
	int i;

	for (i = 0; string[i] != '\0'; i++)
	{
		;
	}
	return (i);
}
